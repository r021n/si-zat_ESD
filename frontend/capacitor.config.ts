import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sizat.esd',
  appName: 'si-zat_ESD',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
